<h1 style="font-size:20px; color:green">create your Article</h1>

<%= form_with model: @article do |a| %>
  <%= a.label :title %>
  <%= a.text_field :title %>
  <%= a.label :content %>
  <%= a.text_field :content %>
  <%= a.submit %>
<% end %>
