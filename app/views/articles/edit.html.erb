<h1 style="font-size:20px; color:green">Edit</h1>

<%= form_with model: @article do |a| %>
  <%= a.label :title %>
  <%= a.text_field :title %>
  <%= a.label :content %>
  <%= a.text_field :content %>
  <%= a.submit %>
<% end %>

<%= link_to "See Articles", articles_path%>
